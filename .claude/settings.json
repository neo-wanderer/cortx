{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "cargo clippy -- -D warnings 2>&1 || { echo '{\"continue\":false,\"stopReason\":\"clippy check failed — fix warnings before committing\"}'; exit 1; }",
            "statusMessage": "Running clippy pre-commit check...",
            "timeout": 60
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -E '\\.rs$' | { read -r f; rustfmt \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Formatting Rust file..."
          }
        ]
      }
    ]
  }
}
